name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build_and_deploy:
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies (Admin)
        working-directory: apps/admin_flutter
        run: flutter pub get

      - name: Install dependencies (Candidate)
        working-directory: apps/candidate_flutter
        run: flutter pub get

      - name: Build web (Admin)
        working-directory: apps/admin_flutter
        run: flutter build web --base-href /talentdock-admin-ui/

      - name: Build web (Candidate)
        working-directory: apps/candidate_flutter
        run: flutter build web --base-href /talentdock-admin-ui/candidate/

      - name: Prepare deployment directory
        run: |
          mkdir -p deploy/candidate
          cp -r apps/admin_flutter/build/web/* deploy/
          cp -r apps/candidate_flutter/build/web/* deploy/candidate/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'deploy'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
